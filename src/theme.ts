// MUI (Material-UI) in a React project can significantly enhance the look and feel of your application, 
// by providing pre-designed components that follow the Material Design guidelines.

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1976d2',
    },
  },
});

export default theme;
